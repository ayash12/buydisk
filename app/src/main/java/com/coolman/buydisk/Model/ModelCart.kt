package com.coolman.buydisk.Model

class ModelCart {
    var produk :String = "defaultValue"
    var price : String = "defaultValue"
    var jumlah: String = "defaultValue"
    var photo : Int=0

        get() = field
        set(value) { field = value }
}